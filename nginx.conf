events {}
http {
    server {
        listen 80;
        location / {
            default_type text/plain;
            return 200 "Hello from Kubernetes Cluster and Deployed App using ArgoCD!\n";
        }
    }
}
